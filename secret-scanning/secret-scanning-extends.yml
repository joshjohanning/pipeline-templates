parameters:
  steps: []

resources:
  repositories:
  - repository: templates
    type: git
    name: pipeline-templates
  - repository: secretscanning
    type: git
    name: secret-scanning-config

steps:

- template: secret-scanning/secret-scanning-steps.yml@templates

${{ parameters.steps }}